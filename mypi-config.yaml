default: default

profiles:
  default:
    cmd: pi
    bundles:
      - mode
      - code-review
      - dynamic-skills
      - btw
      - loop
      - render-raw
      - repo-explorer
      - overview
  reviewer:
    cmd: pi -p
    bundles:
      - code-review-prompt
  review-agent-trajectory:
    cmd: pi -p
    bundles:
      - review-agent-trajectory-prompt